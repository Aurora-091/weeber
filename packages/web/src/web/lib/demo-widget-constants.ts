/**
 * Real demo-call widget (2026-08-27) — frontend copy of the demo org id. Frontend never imports
 * backend runtime (AGENTS.md), only the `AppType` RPC type, so this can't import
 * `packages/api/src/voice/demo-widget-constants.ts` directly — it's duplicated here and MUST
 * match that file's `DEMO_ORG_ID` exactly.
 */
export const DEMO_ORG_ID = "weeber-live-demo";
