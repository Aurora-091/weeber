# Changelog — September 2026

## 2026-09-05 — Personas must not order tools the call may not have (ADR-127)

The 09-05 founder calls failed on prompts as well as the pipeline. Insurance runtimes still named
`crmSync` after ADR-122 withheld the tool; appointment-setter closings said "You're connected"
(ADR-105); post-sale had no closing for documents-not-received and hung up silent (ADR-124). Call
control now names `crmSync` only when it is on the live tool list; `"start"` recomposes after
CRM-withhold the same way it does for a blocked transfer. Re-seed `agentTemplates` on deploy.

## 2026-09-05 — Use the vendor pipeline signals we were already paying for (ADR-126)

Deepgram: concatenate `is_final` chunks on `speech_final` (last Results is often only the tail);
forward `SpeechStarted` so barge-in can count one frame earlier, but never cut on VAD alone.
Telephony: Twilio `mark` / Plivo `checkpoint` after spoken turns (silence/hangup still capped by
the character estimate); Plivo `clearedAudio` is a barge-in ack, not `stop`. Cartesia: `cancel: true`
before closing the TTS socket on barge-in. Exotel: pass through mu-law by default; transcode L16
only when `start` says so. Fresh transport adapter per call.

## 2026-09-05 — Cartesia must not wait three seconds to speak (ADR-125)

Appointment-setter dead air (LLM text, zero TTS bytes) after ADR-124. Cartesia's websocket
defaulted `max_buffer_delay_ms` to 3000 when omitted. We now send 180ms on every continuation
and `flush: true` with `continue: false` on `endTurn`. TTS socket reuse across turns is still
Phase C1 (ADR-083 idle timeout).

## 2026-09-05 — Empty hangUp is not "didn't catch that" (ADR-124)

Two insurance test calls after ADR-123 conversed, then failed in different ways. Post-sale welcome
called `hangUp` with no closing line (documents never arrived) and the empty-turn path still spoke
the hearing apology. Skip `FALLBACK_REPLY` when `hangUp` or `transferToHuman` already ran this
turn. Appointment-setter dead air (LLM text, zero TTS bytes) is not this fix.

## 2026-09-05 — Twilio AMD hijacked a live call (ADR-123)

A test call that was conversing (two turns, Cartesia) was cut off by a different voice saying
sorry to have missed the person. `POST /api/voice/amd-status-callback` had classified the live
human as a machine and Twilio `<Say>` took the stream. AMD now defaults on for US NANP only;
dashboard test calls pass `amd: false`; a `machine_*` result is ignored once the caller has spoken.

## 2026-09-05 — First-token timeout vs tools (ADR-122)

Live call logs looked like a hearing problem. They were not. STT/TTS were fine; insurance turns called
`crmSync` with no CRM connected, the 2.5s first-token bound aborted the model mid-tool, and the agent
spoke "Sorry, I didn't quite catch that." Fix: defer that abort when a tool started this turn; omit
`crmSync` unless the org has connected credentials; rewrite insurance runtime personas tag-free
(ADR-065). Re-seed `agentTemplates` on deploy for the new persona bytes; an editor-pasted persona is
unchanged until saved again.

## 2026-09-05 — Stale frontend/backend audit cleanup

- **Dead code:** Shrunk the knip ratchet baseline from 60 → 2 findings. Removed orphaned web
  modules (`useAudioLevel`, Electron desktop scaffolding, dead `canvas/index.ts` barrel), trimmed
  unused shadcn re-exports, narrowed `voice/workflows/index.ts` and `voice/turn-detection/index.ts`
  barrels, deleted unused error classes and credential-vault helpers, and removed dead audio/graph
  exports (`pcm16ToWav`, `MERGE_TAGS` in API).
- **Contract gap:** Merchant Leads page now surfaces the hosted form URL (`/f/{orgId}`) and
  ingest API-key management (list/create/revoke via `/api/app/leads/api-keys`).
- **Types:** Completed `vite-env.d.ts` with all `VITE_*` variables used in the web package.
- **Docs:** Fixed stale `verticals.ts` reference (`analytics.tsx` → `home.tsx`); updated dead-code
  counts in `progress.md` and `testing.md`.
