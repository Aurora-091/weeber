# Architecture Decision Records — Index

The *why* behind consequential technical and product decisions in this repo — one entry per
decision, dated, with context, the decision itself, and its consequences/tradeoffs. Loosely
follows Michael Nygard's [ADR pattern](https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions).
A shipped ADR is never rewritten; if a decision is later reversed, a new ADR supersedes it and says so.

> Split from the former monolithic `DECISIONS.md` (2026-07-18) so agents load one decision at a time. Each ADR is now its own file with frontmatter (number, title, date, status). Append new decisions as `adr-NNN-slug.md` and add a row here.


| # | Decision | Date | Status |
|---|---|---|---|
| [ADR-001](./adr-001-self-hosted-pipeline-over-a-managed-voice-agent-platform.md) | Self-hosted pipeline over a managed voice-agent platform | 2026-07-04 | Accepted |
| [ADR-002](./adr-002-cartesia-as-the-default-tts-provider-elevenlabs-kept-as-an-o.md) | Cartesia as the default TTS provider, ElevenLabs kept as an option | 2026-07-04 | Accepted |
| [ADR-003](./adr-003-compliance-is-enforced-by-default-not-left-as-an-integration.md) | Compliance is enforced by default, not left as an integration step | 2026-07-04 | Accepted |
| [ADR-004](./adr-004-hipaa-support-is-a-guardrail-not-a-certification.md) | HIPAA support is a guardrail, not a certification | 2026-07-04 | Accepted |
| [ADR-005](./adr-005-groq-added-as-a-swappable-llm-provider-behind-the-same-patte.md) | Groq added as a swappable LLM provider behind the same pattern as TTS | 2026-07-04 | Accepted |
| [ADR-006](./adr-006-workflows-as-code-first-json-config-not-a-visual-builder.md) | Workflows as code-first JSON config, not a visual builder | 2026-07-04 | Accepted |
| [ADR-007](./adr-007-national-dnc-registry-integration-deferred-internal-list-is-.md) | National DNC Registry integration deferred; internal list is the enforced mechanism today | 2026-07-04 | Accepted |
| [ADR-008](./adr-008-public-endpoint-via-a-free-cloudflare-quick-tunnel-is-a-temp.md) | Public endpoint via a free Cloudflare quick-tunnel is a temporary stopgap, not a solution | 2026-07-04 | Accepted |
| [ADR-009](./adr-009-evaluated-and-rejected-livekit-agents-as-an-orchestration-la.md) | Evaluated and rejected LiveKit Agents as an orchestration layer | 2026-07-04 | Accepted |
| [ADR-010](./adr-010-extracted-the-compliance-layer-into-a-standalone-framework-a.md) | Extracted the compliance layer into a standalone, framework-agnostic package | 2026-07-05 | Accepted |
| [ADR-011](./adr-011-v1-3-hardening-pass-auth-signature-validation-retry-cap-fix-.md) | v1.3 hardening pass: auth, signature validation, retry-cap fix, rate limiting | 2026-07-05 | Accepted |
| [ADR-012](./adr-012-structured-call-state-as-ground-truth-not-the-transcript.md) | Structured call state as ground truth, not the transcript | 2026-07-05 | Accepted |
| [ADR-013](./adr-013-named-cloudflare-tunnel-replaces-the-quick-tunnel-supervisor.md) | Named Cloudflare tunnel replaces the quick-tunnel supervisor | 2026-07-05 | Accepted |
| [ADR-014](./adr-014-reverted-adr-013-back-to-the-quick-tunnel-named-tunnel-parke.md) | Reverted ADR-013: back to the quick-tunnel, named tunnel parked | 2026-07-05 | Superseded/Reverted |
| [ADR-015](./adr-015-open-core-framework-not-a-pure-library-or-a-pure-hosted-plat.md) | Open-core framework, not a pure library or a pure hosted platform | 2026-07-05 | Accepted |
| [ADR-016](./adr-016-self-hosted-reframed-as-a-three-tier-spectrum-not-a-binary-c.md) | "Self-hosted" reframed as a three-tier spectrum, not a binary claim | 2026-07-05 | Accepted |
| [ADR-017](./adr-017-compliance-audit-trail-export.md) | Compliance audit-trail export | 2026-07-06 | Accepted |
| [ADR-018](./adr-018-switch-from-mit-to-a-fair-code-license-vent-sustainable-use-.md) | Switch from MIT to a fair-code license (Vent Sustainable Use License) | 2026-07-07 | Accepted |
| [ADR-019](./adr-019-full-rebrand-from-vent-to-openvent.md) | Full rebrand from "Vent" to "OpenVent" | 2026-07-08 | Accepted |
| [ADR-020](./adr-020-landing-page-storytelling-rebuild-real-logos-scroll-driven-d.md) | Landing page storytelling rebuild: real logos, scroll-driven diagram, drop stale demo data | 2026-07-08 | Accepted |
| [ADR-021](./adr-021-telephony-provider-abstraction-deferred-scoped-documented-no.md) | Telephony provider abstraction: deferred, scoped, documented (no code this round) | 2026-07-08 | Accepted |
| [ADR-022](./adr-022-per-call-latency-breakdown-first-value-only-persisted-once-a.md) | Per-call latency breakdown: first-value-only, persisted once at call end | 2026-07-08 | Accepted |
| [ADR-023](./adr-023-cross-call-memory-flat-key-value-overlay-merged-not-replaced.md) | Cross-call memory: flat key/value overlay, merged not replaced | 2026-07-08 | Accepted |
| [ADR-024](./adr-024-fixed-a-repo-wide-silent-typecheck-gap-tsc-noemit-was-checki.md) | Fixed a repo-wide silent typecheck gap: `tsc --noEmit` was checking nothing | 2026-07-08 | Accepted |
| [ADR-025](./adr-025-multi-user-dashboard-auth-labeled-api-keys-not-accounts.md) | Multi-user dashboard auth: labeled API keys, not accounts | 2026-07-08 | Accepted |
| [ADR-026](./adr-026-redis-backed-session-storage-optional-opt-in-async-interface.md) | Redis-backed session storage: optional, opt-in, async interface either way | 2026-07-08 | Accepted |
| [ADR-027](./adr-027-docs-landing-page-sync-after-the-four-item-round.md) | Docs/landing-page sync after the four-item round | 2026-07-08 | Accepted |
| [ADR-028](./adr-028-relicense-from-the-vent-sustainable-use-license-back-to-apac.md) | Relicense from the Vent Sustainable Use License back to Apache 2.0, protect the name via trademark instead | 2026-07-09 | Accepted |
| [ADR-029](./adr-029-strip-runable-scaffold-leftovers-analytics-beacon-dead-templ.md) | Strip Runable-scaffold leftovers: analytics beacon, dead template files, placeholder identifiers | 2026-07-09 | Accepted |
| [ADR-030](./adr-030-fork-into-a-private-repo-as-weeber-s-backend-add-org-lite-sc.md) | Fork into a private repo as Weeber's backend, add org-lite scoping + the Shopify vertical (cart recovery, COD confirmation, feedback) | 2026-07-09 | Accepted |
| [ADR-031](./adr-031-design-system-codebase-structure-and-vertical-agnostic-data-.md) | Design system, codebase structure, and vertical-agnostic data model for Weeber's dashboards | 2026-07-09 | Accepted |
| [ADR-032](./adr-032-weeber-product-design-system-arc-like-warm-paper-theme-confi.md) | Weeber product design system: Arc-like warm paper theme, confirmed by explicit UI/UX round | 2026-07-09 | Accepted |
| [ADR-033](./adr-033-klaviyo-shopify-flow-research-identified-a-real-gap-entry-co.md) | Klaviyo/Shopify Flow research: identified a real gap (entry-condition branching), generalized beyond Shopify | 2026-07-09 | Accepted |
| [ADR-034](./adr-034-stack-finalization-supabase-postgres-as-the-primary-db-railw.md) | Stack finalization: Supabase Postgres as the primary DB, Railway + Vercel confirmed, Razorpay-first billing (India GTM) | 2026-07-10 | Accepted |
| [ADR-035](./adr-035-backend-separation-prepare-the-seam-now-split-later.md) | Backend separation: prepare the seam now, split later | 2026-07-10 | Accepted |
| [ADR-036](./adr-036-backend-split-executed-packages-api-weeber-api-packages-web-.md) | Backend split executed: `packages/api` (@weeber/api) + `packages/web` (@weeber/web) | 2026-07-10 | Accepted |
| [ADR-037](./adr-037-phase-2-merged-into-phase-1-one-backlog-sequencing-and-gates.md) | Phase 2 merged into Phase 1: one backlog, sequencing and gates preserved | 2026-07-10 | Accepted |
| [ADR-038](./adr-038-india-telephony-exotel-over-twilio-and-the-number-series-rea.md) | India telephony: Exotel over Twilio, and the number-series reality | 2026-07-10 | Accepted |
| [ADR-039](./adr-039-weeber-product-theme-recolored-dark-fully-monochrome-overrid.md) | Weeber product theme recolored: dark, fully monochrome (overrides ADR-032) | — | Supersedes prior |
| [ADR-040](./adr-040-configurable-per-agent-language-multi-provider-stt-tts-sarva.md) | Configurable per-agent language + multi-provider STT/TTS (Sarvam added) | 2026-07-10 | Accepted |
| [ADR-041](./adr-041-supabase-auth-signup-confirmation-link-otp-password-reset-fu.md) | Supabase auth: signup confirmation (link + OTP) + password reset; full waitlist referral system ported from Vocalist | 2026-07-10 | Accepted |
| [ADR-042](./adr-042-per-org-twilio-isolation-real-sub-accounts-byo-sid-auth-toke.md) | Per-org Twilio isolation: real sub-accounts + BYO (SID + auth token + number) | 2026-07-10 | Accepted |
| [ADR-043](./adr-043-passwordless-sign-in-switches-from-magic-link-to-email-otp-c.md) | Passwordless sign-in switches from magic link to email OTP code; Weeber-branded auth emails | 2026-07-10 | Accepted |
| [ADR-044](./adr-044-fixed-theme-weeber-light-dark-color-inversion-bug.md) | Fixed `.theme-weeber` light/dark color inversion bug | 2026-07-11 | Accepted |
| [ADR-045](./adr-045-weeber-marketing-waitlist-page-faithful-visual-port-from-voc.md) | Weeber marketing/waitlist page: faithful visual port from Vocalist, real brand assets | 2026-07-11 | Accepted |
| [ADR-046](./adr-046-dynamic-voice-picker-with-instant-per-voice-preview-replaces.md) | Dynamic voice picker with instant per-voice preview (replaces free-text voice ID) | 2026-07-11 | Accepted |
| [ADR-047](./adr-047-setup-modal-not-a-setup-page-vertical-driven-dashboard-as-th.md) | Setup modal, not a setup page — vertical-driven dashboard as the default landing route (2026-07-12) | 2026-07-12 | Accepted |
| [ADR-048](./adr-048-plivo-exotel-byo-telephony-credential-layer-only-2026-07-12.md) | Plivo + Exotel BYO telephony, credential layer only (2026-07-12) | 2026-07-12 | Accepted |
| [ADR-049](./adr-049-real-plivo-exotel-call-transport-provider-wire-format-abstra.md) | Real Plivo/Exotel call transport — provider wire-format abstraction + a correction (2026-07-12) | 2026-07-12 | Accepted |
| [ADR-050](./adr-050-merchant-impersonation-removed-entirely-2026-07-12.md) | Merchant impersonation removed entirely (2026-07-12) | 2026-07-12 | Accepted |
| [ADR-051](./adr-051-agent-preview-drawer-live-voice-test-call-is-a-parallel-sand.md) | Agent Preview drawer — live voice test call is a parallel sandbox handler, not a 4th telephony provider (2026-07-12) | 2026-07-12 | Accepted |
| [ADR-052](./adr-052-merchant-renamed-to-user-as-the-tenant-facing-actor-term-202.md) | "Merchant" renamed to "User" as the tenant-facing actor term (2026-07-13) | 2026-07-13 | Accepted |
| [ADR-053](./adr-053-correction-to-adr-043-password-reset-also-went-otp-only-rese.md) | Correction to ADR-043: password reset also went OTP-only; `reset-password.tsx` kept as speculative scaffolding (2026-07-13) | 2026-07-13 | Correction |
| [ADR-054](./adr-054-dialogs-sheets-dropdowns-tooltips-selects-render-inside-the-.md) | Dialogs/sheets/dropdowns/tooltips/selects render inside the themed shell via a portal-container context (2026-07-13) | 2026-07-13 | Accepted |
| [ADR-055](./adr-055-agent-console-full-window-layout-via-a-new-appshell-fullblee.md) | Agent console: full-window layout via a new `AppShell` `fullBleed` opt-out (2026-07-13) | 2026-07-13 | Accepted |
| [ADR-056](./adr-056-correction-the-38-pre-existing-test-failures-baseline-cited-.md) | Correction: the "38 pre-existing test failures" baseline cited across many prior sessions was a false signal, not real bugs (2026-07-16) | 2026-07-16 | Correction |
| [ADR-057](./adr-057-pricing-locked-internal-only-not-deployed-geo-differentiated.md) | Pricing locked (internal only, not deployed): geo-differentiated tiers, split by voice cost, minutes not calls (2026-07-18) | 2026-07-18 | Accepted |
| [ADR-058](./adr-058-adopt-supabase-realtime-for-live-dashboard-updates-2026-07-18.md) | Adopt Supabase Realtime for live dashboard updates, replacing polling (2026-07-18) | 2026-07-18 | Accepted (not yet implemented) |
| [ADR-059](./adr-059-testing-infrastructure-web-component-tests-coverage-and-e2e.md) | Testing infrastructure: web component tests (happy-dom), opt-in coverage, and a secret-free Playwright E2E (2026-07-19) | 2026-07-19 | Accepted |
| [ADR-060](./adr-060-indic-language-smart-provider-default-sarvam-and-mid-call-switching-rejected.md) | Indic-language calls smart-default to Sarvam when no provider is chosen; mid-call spoken-language switching stays rejected (2026-07-19) | 2026-07-19 | Accepted |
| [ADR-061](./adr-061-leads-layer-lean-phase-1-relabel-and-orgid-as-hosted-form-token.md) | Native leads layer: lean Phase 1 (ingest + api-keys re-labelled to Phase 2); orgId is the public hosted-form token; Shopify Orders migration deferred (2026-07-19) | 2026-07-19 | Accepted |
| [ADR-062](./adr-062-compliance-audit-trail-legal-shape-and-dial-time-consent.md) | Compliance audit trail — legal-shape completion + dial-time consent enforcement (2026-07-30) | 2026-07-30 | Accepted (Phase I implemented; II–III deferred) |
| [ADR-063](./adr-063-turn-detection-seam-shipped-eot-model-deferred-behind-a-gate.md) | Turn-detection seam shipped (heuristic default, flag-gated, no migration); the EOT model is deferred behind a gate — Phase II health data + staging isolation (2026-07-31) | 2026-07-31 | Accepted |
| [ADR-064](./adr-064-the-merchant-owns-the-discount-amount-the-model-owns-only-the-timing.md) | The merchant owns the discount amount; the model owns only the timing — non-registration as the enforcement mechanism (2026-08-01) | 2026-08-01 | Accepted |
| [ADR-065](./adr-065-values-not-placeholders-prompts-instruct-fact-blocks-supply-values.md) | Values, not placeholders — seeded personas carry instructions only; fact blocks carry values; a runtime scrub is the last line of defense (2026-08-01) | 2026-08-01 | Accepted |
| [ADR-066](./adr-066-a-tool-that-acts-on-a-real-world-entity-is-bound-to-that-entity-server-side.md) | A tool that acts on a real-world entity is bound to that entity server-side — the model never names the target of a destructive action (2026-08-01) | 2026-08-01 | Accepted |
| [ADR-067](./adr-067-one-composition-path-and-a-compiled-prompt-panel-the-editor-shows-what-ships.md) | One composition path, and an editor that shows what actually ships — compiled prompt panel, tool consequence groups, guardrail consequence text (2026-08-01) | 2026-08-01 | Accepted |
| [ADR-068](./adr-068-product-layout-responds-to-the-content-column-not-the-viewport.md) | Product layout responds to the content column, not the viewport — `@container` on AppShell's `<main>`, container variants in every product route (2026-08-01) | 2026-08-01 | Accepted |
| [ADR-069](./adr-069-the-caller-identity-a-tool-writes-to-comes-from-the-carrier-not-the-model.md) | The caller identity a tool writes to comes from the carrier, not the model — `crmSync` upsert key bound server-side from `humanNumber` (2026-08-01) | 2026-08-01 | Accepted |
| [ADR-070](./adr-070-one-voice-per-call-failover-is-sticky-and-a-voice-id-never-crosses-providers.md) | One voice per call: TTS failover is sticky for the rest of the call, and a voice ID never crosses providers (2026-08-05) | 2026-08-05 | Accepted |
| [ADR-071](./adr-071-ending-a-call-is-a-local-guarantee-the-provider-rest-hangup-is-best-effort.md) | Ending a call is a local guarantee — closing the media WebSocket is authoritative, the provider REST hangup is best-effort and cannot throw (2026-08-05) | 2026-08-05 | Accepted |
| [ADR-072](./adr-072-a-provider-contract-is-what-the-server-accepts-not-what-its-docs-say.md) | A provider contract is what the server accepts, not what its docs say — and a provider that hears nothing must not report itself healthy (2026-08-05) | 2026-08-05 | Accepted |
| [ADR-073](./adr-073-a-repair-path-with-no-caller-is-not-a-repair-path.md) | A repair path with no caller is not a repair path — `syncNumberWebhooksForOrg` gets an admin route, manual on purpose (2026-08-06) | 2026-08-06 | Accepted |
| [ADR-074](./adr-074-clearing-a-timer-does-not-cancel-a-timeout-that-already-fired.md) | Clearing a timer does not cancel a timeout that already fired — `callerSpeechEpoch` re-checked after every await in `handleSilenceTimeout` (2026-08-06) | 2026-08-06 | Accepted |
| [ADR-075](./adr-075-a-required-check-must-assert-what-succeeded-not-what-failed.md) | A required check must assert what succeeded, not what failed — `ci-success` allow-lists `result == "success"` so a gate cannot go green on jobs that never ran (2026-08-08) | 2026-08-08 | Accepted |
| [ADR-076](./adr-076-a-start-command-that-hides-its-failure-is-not-shippable.md) | A start command that hides its failure is not shippable — production migrations run through `scripts/migrate.ts` (drizzle-orm migrator, `prepare: false`, full Postgres error + cause chain) instead of the `drizzle-kit` CLI, which exits 1 silently; both deploy triggers gated on `checkSuites` (2026-08-08) | 2026-08-08 | Accepted |
| [ADR-077](./adr-077-a-201-is-not-proof-the-resource-is-configured.md) | A 201 is not proof the resource is configured — `buyNumberForOrg` reads the webhooks back off Twilio's create response, re-applies them on mismatch, records the number either way, and a wire-level test asserts the serialised request instead of our own arguments (2026-08-08) | 2026-08-08 | Accepted |
| [ADR-078](./adr-078-a-rename-is-a-set-of-live-contracts-not-a-find-and-replace.md) | A rename is a set of live contracts, not a find-and-replace — the OpenVent → Weeber rename executed per category: the caller-facing persona rewritten (not swapped), `X-OpenVent-Admin-Key` accepted permanently alongside `X-Weeber-Admin-Key`, `@weeber/compliance` renamed down to its relative-path test mock, and historical records left as written (2026-08-08) | 2026-08-08 | Accepted |
| [ADR-079](./adr-079-a-surface-that-can-spend-must-be-able-to-refund.md) | A surface that can spend must be able to refund, and "reset" must not strand what it paid for — the admin panel gains `POST /orgs/:orgId/twilio/numbers/:id/release` as the inverse of its purchase route, `resetToPlatformDefault` refuses with 409 rather than orphaning billable numbers, and the "`closeOrgTelephony` has zero callers" triage note is corrected (2026-08-08) | 2026-08-08 | Accepted |
| [ADR-080](./adr-080-a-permission-check-that-cannot-fail-is-not-a-permission-check.md) | A permission check that cannot fail is not a permission check — `/telephony/reset` stays deliberately ungated because `org_members` is unique on the user alone and every role is `"owner"`, making `/org/close`'s 403 and `settings.tsx`'s `isOwner` unreachable; the five misleading sites are labelled, and dropping `org_members_user_idx` (which is what team invites require) is recorded as reintroducing the first-login race it was added to fix (2026-08-09) | 2026-08-09 | Accepted |
| [ADR-081](./adr-081-an-ai-that-performs-the-licensed-act-is-the-unlicensed-agent.md) | An AI that performs the licensed act *is* the unlicensed agent — the regulated boundary is the product, not a limitation of it: the agent qualifies and warm-transfers, and may not claim licensure, recommend a carrier, quote premiums, itemize health conditions, collect SSN/DOB/routing/account numbers, set effective date or beneficiary, or take a voice-signature ACH authorization | 2026-08-09 | Accepted |
| [ADR-082](./adr-082-a-transfer-outranks-a-hangup.md) | A transfer outranks a hang-up — a queued `transferToHuman` wins over a concurrent `hangUp` instead of racing it | 2026-08-09 | Accepted (not live-verified) |
| [ADR-083](./adr-083-a-tts-socket-nobody-has-spoken-on-is-not-a-broken-provider.md) | A TTS socket nobody has spoken on is not a broken provider — TTS connects lazily on first text, so an idle socket no longer trips failover and burn a voice identity | 2026-08-09 | Accepted |
| [ADR-084](./adr-084-a-call-the-caller-was-never-heard-in-is-not-healthy.md) | A call the caller was never heard in is not healthy — call health counts `callerTranscriptCount`, not just whether the pipeline threw | 2026-08-09 | Accepted |
| [ADR-085](./adr-085-an-outbound-agent-that-cannot-name-who-it-called.md) | An outbound agent that cannot name who it called — lead greeting context is resolved in the pickup `Promise.all` and bound into the opener | 2026-08-09 | Accepted (amended by ADR-087) |
| [ADR-086](./adr-086-a-template-written-for-one-account-should-not-be-in-everyone-elses-agent-list.md) | A template written for one account should not be in everyone else's agent list — `agentTemplates` gains `visibility` + `ownerOrgId`, plus `POST /admin/orgs/:orgId/agents/grant`; a grant survives re-seed | 2026-08-09 | Accepted |
| [ADR-087](./adr-087-a-merge-tag-no-ingest-path-can-fill-is-not-bound.md) | A merge tag no ingest path can fill is not bound — the intake schema carries `interest_area` and `state`, the two fields the opener actually needs (amends ADR-085) | 2026-08-09 | Accepted |
| [ADR-088](./adr-088-a-guard-with-no-callers-is-documentation.md) | A guard with no callers is documentation — the prohibited-capture guard is enforced at the write path: `captureField.execute` refuses and says why, `logToolCall` redacts before both the `tool_calls` insert and the webhook, refusals log a `guardrail_event` with the key and never the value | 2026-08-09 | Accepted |
| [ADR-089](./adr-089-an-import-that-cannot-be-previewed-is-tested-on-a-live-call.md) | An import that cannot be previewed is tested on a live call — `POST /api/leads/ingest/csv` defaults to `dryRun: true`, reports per-column mapping/regulated-column refusals/row issues by spreadsheet line, and refuses bare national numbers without a `defaultCountryCode` | 2026-08-09 | Accepted |
| [ADR-090](./adr-090-the-repo-had-no-way-to-see-that-code-was-never-called.md) | The repo had no way to see that code was never called — `knip` added as a CI **ratchet** (baseline 61 findings, fails only on new ones), because 8 of ADRs 073–088 are the same wiring defect, unit tests structurally hide it, and no reachability tool existed | 2026-08-09 | Accepted |
| [ADR-091](./adr-091-visibility-was-a-browse-filter-not-an-authorization-boundary.md) | Visibility was a browse filter, not an authorization boundary — ADR-086's predicates covered 4 of ~10 reads, so every by-key path (`templateKey` is a URL path param) still returned another org's `defaultPersonaPrompt` and the AI-draft prompt enumerated every private key; `loadVisibleTemplate`/`isTemplateVisibleToOrg` are now the only supported by-key reads, enforced at the route boundary *and* in the resolvers, with not-found and not-visible conflated to one 404 and a real FK on `orgAgentConfigs.templateKey` | 2026-08-10 | Accepted |
| [ADR-092](./adr-092-the-pause-switch-changed-a-dashboard-counter-and-nothing-else.md) | The pause switch changed a dashboard counter and nothing else — `org_agent_configs.enabled` was read in 2 cosmetic places and 0 on the call path, so a paused agent still dialled; `isAgentDispatchable` is now a 7th scheduler gate (cancel, don't defer), enforced there rather than in `resolveAgentConfig` (shared with the test surfaces) or `placeOutboundCall` (whose only `agentKey` callers are the test-call endpoints), failing OPEN for a missing config row and for a non-template persona, and folding in the cross-vertical leftover rows that `getAgentConfigsForOrg` hides but the resolver still reaches | 2026-08-10 | Accepted |
| [ADR-093](./adr-093-a-vertical-switch-left-the-old-verticals-agents-live.md) | A vertical switch left the old vertical's agents live — `PATCH /api/app/settings` changed `orgs.vertical` with no cleanup, so an org that moved verticals kept the old vertical's `org_agent_configs` rows `enabled=t` (one holding an active caller ID), invisible in the agents grid because `getAgentConfigsForOrg` narrows by vertical yet reachable by the resolver because ADR-091's `visibleTemplatesForOrg` deliberately does not; `retireOffVerticalAgentConfigs` now disables them and releases `phoneNumberId` on the switch itself — disable, never delete, because the persona text is the merchant's work and the switch is reversible — and inbound calls to a paused agent are decided to keep answering on the default persona, `enabled` being an outbound gate only | 2026-08-10 | Accepted |
| [ADR-094](./adr-094-a-sentence-with-a-hole-in-it-is-not-a-safer-sentence.md) | A sentence with a hole in it is not a safer sentence — G1.3 strips unresolved `{{tags}}` from the composed prompt and assumed the model "handles gracefully" the residue, but on 2026-08-10 three of four calls spoke the damage in their first sentence ("is this **?**", "is this **[Caller Name]**? This is **[Agent Name]** with presistentads" — a string that exists nowhere in repo or DB, so the model invented it), and one SMS body shipped `[Advisor Desk Number]`; deleting a hole removes the label on the hole, not the hole, so the fill is less constrained rather than safer. Scrub stays as the net; the decision is that an instruction the runtime cannot complete is replaced by an authored **tagless alternate line**, never by a damaged copy of itself, tags are only permitted where a producer exists (enforced at seed/save, not on a live call), and the unresolvable case stops being a silent LLM fallback — which also closes audit 13's P0, the literal-greeting fast path being 0-for-11 in production at p50 1539 ms of avoidable TTFT | 2026-08-10 | Proposed |
| [ADR-095](./adr-095-the-callees-country-code-is-not-our-customers-market.md) | The callee's country code is not our customer's market — Weeber has a first-class `vertical` axis (ADR-031) and no equivalent for **market**, yet market is doing real work in a code comment (`agent-frame.ts:53` "India-first list since that's the primary market"), a locked pricing decision (India and Global as separate plan families, not a currency toggle) and a prompt-authoring doc ("US, English-only") — while `orgs.country_code`/`currency`/`timezone` are write-only (accepted by `PATCH /settings`, echoed by `GET /me`, read by zero code under `voice/`, NULL on 3 of 4 prod orgs) and the only market signal in the system is `isIndianNumber(callee)` in `calling-window.ts:30`, which is the right input for a calling window and the wrong one for regulator, template eligibility, spoken currency, default language, provider default and plan family; that single inference is why the 19,480-char explicitly-US-only final-expense template is `enabled` on 2 of 3 insurance orgs including the only `country_code=IN` one. `orgs.market` (+ optional per-agent override, + nullable `agent_templates.market` folded into ADR-091's `loadVisibleTemplate`) becomes the field the runtime reads and is emitted as its own prompt segment rather than prose inside a persona blob; the callee prefix keeps deciding the calling window unchanged and is demoted elsewhere to a mismatch detector. Does not decide US-vs-India primacy, touches no `weeber-compliance` code, and does not reopen mid-call language switching (ADR-060 stands) | 2026-08-10 | Proposed |
| [ADR-096](./adr-096-a-gate-on-two-of-five-doors-is-a-door.md) | A gate on two of five doors is a door — `placeOutboundCall` has five callers and only the scheduler (`workflows/scheduler.ts:119`) and the campaign route (`voice/routes.ts:313`) run the six compliance gates, while `POST /api/leads/:id/call-now` (`app/routes.ts:957`), the tenant preview test-call (`app/routes.ts:645`) and the admin `.../test-call-phone` (`voice/routes.ts:840`) dial with none of them — and `place-outbound-call.ts:80-88` documents the opposite ("both call sites already run them before reaching here") while containing zero gate calls; with an AI voice treated as "artificial" under the TCPA since the FCC's Feb 2024 ruling, an ungated dial is $500–$1,500 per call with no aggregate cap, so the gates move **inside** `placeOutboundCall` behind one `assertOutboundCallAllowed(orgId, to)` chokepoint that **fails closed**, the existing pre-dial checks stay (a caller that already knows it will be refused should not spend a Twilio leg to find out), the misleading doc comment is rewritten as an invariant, and a source-level regression test asserts every caller reaches the chokepoint. Deliberately touches only `packages/api/src/voice/compliance/`, not `packages/weeber-compliance` | 2026-08-10 | Accepted (implemented 2026-08-10) |
| [ADR-097](./adr-097-the-market-that-answered-is-not-the-market-we-planned.md) | The market that answered is not the market we planned — `project-brief.md`'s "launch vertical: ecommerce, Shopify first" and the India-first framing throughout the code are superseded by what actually happened: **no India pilot was ever landed**, one real US-licensed insurance agency is in pilot dialling US prospects with ~5 more on a waitlist, and `calls` shows 11 rows all-time with `to_number` `+91` on every one — so **US insurance outbound is the launch market and vertical**, and the India/Shopify plan is parked (not deleted) behind ADR-031's vertical seam with ADR-095's `orgs.market` shipped as the field that makes "parked" expressible instead of implicit. The grant and program narrative (Sarvam Startup Program, NVIDIA Inception, DPIIT) is restated as an Indian company selling into the US rather than an India-market company. Six preconditions before the first real campaign — ADR-096's chokepoint, a non-empty `insurance_advisors` roster with lead-`state`-over-area-code precedence, a DNC feed, consent provenance captured at CSV import, one successful real US call, and a billing gateway — plus the conditions under which India reopens (a 1600-series-capable carrier and an IRDAI-regulated design partner, neither of which exists today) | 2026-08-10 | Proposed |
| [ADR-098](./adr-098-an-empty-roster-is-not-a-claim-of-no-coverage.md) | An empty roster is not a claim of no coverage — one day after ADR-096 put the six gates on all five dial paths, the empty `insurance_advisors` table (0 rows, every prod org) stopped being a partial inconvenience and became a total US dialing block for every insurance org including both test-call endpoints, so the only real pilot customer and every waitlist demo were locked out; the gate is not too strict, it is asking the wrong question of the wrong input — it already fails open when `resolveUsState` cannot map the area code at all, so an org that merely had not filled in a form was treated more harshly than a number whose state is entirely unknown, and "no roster" was being collapsed into "negative data" when the two are different facts (a roster listing FL/GA dialing NY has asserted coverage that excludes the call; an empty roster has asserted nothing), on top of an area-code state inference that number portability makes wrong in both directions (audit 16 F5, unfixed). So an **empty** roster allows and logs an unconditional greppable warning naming the org and the unverified state, while **any** advisor row (including one with `licensed_states: []`) enforces exactly as before. The ADR-096 chokepoint stands, DNC stays hard with no bypass on any path, the calling window and FTSA attempt cap stay on — the rejected alternative, enabling `callingWindowTestModeUntil` on the pilot org, was worse on every axis: it also lifts the calling window, expires silently at 24h mid-campaign, and is a toggle someone must remember. Roster entry becomes an onboarding problem, which is where it belonged | 2026-08-11 | Accepted |
| [ADR-099](./adr-099-a-font-fetched-at-render-time-is-not-a-pinned-input.md) | A font fetched at render time is not a pinned input — `main` was red for four straight commits on `visual`, `fonts` and `CI success` while the entire range changed exactly one file under `packages/web` (a two-line test), because `styles.css:1` `@import`ed the Google Fonts CSS2 endpoint: every one of the 78 pixel baselines was a photograph of whatever binary the CDN served that minute, and upstream Fraunces moved. The failure sets disagreed per machine (6/78 shots in CI, 1/78 locally; 2/26 provenance routes in CI, 0/26 locally), which is what measuring the machine looks like, and the one reproducible diff was a 28px band where the wordmark rendered in a lighter optical grade of the same typeface. This also falsified one of the four pins in `playwright.visual.config.ts` that justify local baseline regeneration — "the font FILES are pinned to webfonts" proves a glyph came from *a* webfont, not from the *same* one as last week. The four brand families are now `@fontsource-variable` packages at `5.3.0` pinned in `bun.lock`, bundled same-origin by Vite (`standard` = the `opsz`+`wght` axes the CDN was serving), and `ALLOWED_OFF_ORIGIN` in the screenshot guard drops to **empty**, so a reintroduced CDN font fails the suite instead of moving the baselines again. **Zero baseline bytes changed** — the npm-pinned build is the one the baselines were made from, so pinning restored the prior rendering rather than laundering the drift into it; no `ALLOWED` entry, no `maxDiffPixels`, no ratchet widened | 2026-08-11 | Accepted |
| [ADR-100](./adr-100-the-hot-path-was-carrying-work-the-caller-could-not-hear.md) | The hot path was carrying work the caller could not hear — measured before changing anything (44 complete turns: v2v p50 **1863ms**, p95 4394; one decomposed turn is 8% pre-LLM / **71% LLM TTFT** / 21% TTS), corrected two of my own earlier readings (the numbers are *not* geographically poisoned — `voiceToVoiceMs` runs `speech_final`→first TTS byte, so Twilio's ocean leg is outside both endpoints; and `tts_first_byte_ms` is cumulative from turn start, not the TTS stage), then fixed only what was being paid for nothing: the caller-transcript INSERT came off the hot path where it sat between `speech_final` and the LLM request, cross-region Singapore→Mumbai, feeding a table the model never reads — **chained** through one promise rather than fire-and-forget, because rows are read back ordered by identity and racing inserts would store an agent line before the caller line it answers, with a 2000ms-capped drain in `finalizeCall` so a `hangUp`-terminated call keeps the lines explaining why it ended; the literal-greeting guard, silent while rejecting **11 of 11 calls ever placed** and charging every one ~1.3s of avoidable TTFT, now warns with the unresolved tag names (data defect — 3 of 4 `leads` rows are `name = NULL`, `fields = {}` — not a code one, and not ADR-094's authored tagless line, which is still unbuilt); merchant free text is trimmed where it becomes speech because prod holds `"alice "` and TTS can voice the doubled space as a stumble; and `{{interaction_type}}` gets a producer (intake field + CSV aliases) on ADR-087's exact precedent, rather than rewording an audited spoken line, which would be a compliance review. Deferred as unearned: the LLM TTFT fat tail (needs per-request gateway timing), the 10/78 turns that reserved an index and produced no audio, Cartesia-vs-ElevenLabs at n=2, and any write to prod data | 2026-08-12 | Accepted |
| [ADR-101](./adr-101-a-reply-too-short-to-trip-the-buffer-was-never-spoken.md) | A reply too short to trip the buffer was never spoken — ADR-100 deferred "10/78 turns reserved an index and produced no audio" as dead air; reconciling every `turn_latency` row against the same call's `transcripts` shows it was **two** things, and the label hid the one that mattered: 9 rows with all three columns NULL are turns the caller aborted **before the first LLM token** (provable, not inferred — call 25 has 27 turn rows and 23 agent lines, and the 4 all-NULL rows are exactly the 4 turns with no agent line, each sitting between two caller lines under 1.5s apart), plus 8 more rows at 405–439ms with no `llm_ttft_ms` that are just `speakCannedLine` re-prompts; the **1** real defect is call 21 turn 3 — `llm_ttft_ms = 2779`, `tts_first_byte_ms` NULL, transcript recording `"OK."` as spoken, caller transferred having heard silence. Cause: ADR-082's tone-tag filter held streamed deltas back until it could rule out a leading `[[tone:value]]` marker and released on the first of three conditions (tag matched / any `]]` / 24 chars) with **no condition for end of stream**, so a reply shorter than the cap with no `]]` was still entirely in the buffer when the turn ended, ADR-083's lazy connect meant no socket existed, `endTurn()` took its "no speakable text" branch, and nothing errored or logged — every short untagged reply ("Sure, one moment.", "Got it, thanks.") was exposed, rare only because the model usually does emit the tag. Third defect in this one feature (ADR-082 unwired `setTone`, ADR-083 the socket lifetime) and ADR-090's class at its purest: a closure in a 2000-line `stream.ts` that no test could reach, with `tone-tags.test.ts` covering only the pure function underneath it. Fix: the state machine becomes `createToneTagFilter` in `tone-tags.ts` with an idempotent `flush()` called in `speak()`'s `finally` before `endTurn()` — **not** on a barge-in, where abandoning the text is correct, and a malformed partial tag is spoken as-is because audible garbage beats silence — plus the alarm that was missing (`DEAD AIR on turn N` when a turn produced text and zero audio bytes), since a NULL `tts_first_byte_ms` was indistinguishable from three benign causes, which is exactly why ADR-100 mislabelled all ten. Rejected: dropping the hold-back (costs every turn ~1s of p50 to fix 1 in 78), lowering the cap (must exceed `[[tone:empathetic]]`; only moves the boundary), retrying untagged turns, and a `turn_outcome` column (right answer, deferred until the taxonomy this ADR establishes survives more than 11 calls). Also surfaced, deliberately not fixed: the silence re-prompt fired 3× in call 25 and the caller answered within ~3s each time — the agent interrupts people who are thinking | 2026-08-12 | Accepted |
| [ADR-102](./adr-102-a-voice-belongs-to-an-agent-not-a-deployment.md) | A voice belongs to an agent, not a deployment — ADR-070 established that a voice ID never crosses providers, but underneath it every TTS adapter still read `voiceIdOverride \|\| process.env.<PROVIDER>_VOICE_ID`, which reintroduces the same hazard one layer lower: a voice is an **agent** property (`org_agent_configs.voice_provider` + `voice_id`, chosen in the dashboard picker) while an env var is a **deployment** property, so the same agent row could speak as a different person depending on which environment served the call. Not theoretical — of 43 Railway production vars, `ELEVENLABS_VOICE_ID` and `SARVAM_VOICE_ID` are **absent**, all 6 prod `org_agent_configs` rows are Cartesia with `tts_fallback_order` null so `DEFAULT_TTS_FALLBACK_ORDER = [cartesia, elevenlabs, sarvam]` governs **every call ever placed**, and its second leg resolved to `undefined` and built `wss://api.elevenlabs.io/v1/text-to-speech/undefined/stream-input`: the declared cross-provider TTS failover has never worked, said nothing at boot or on the call record, and would first have been discovered during a Cartesia incident — ADR-090's class again. Fix: `FALLBACK_VOICE_BY_PROVIDER` as a `Record<TtsProvider, string>` **code constant** in `tts/default-voices.ts`, because a missing env var is `undefined` mid-call in production while a missing constant does not typecheck; all three entries verified live, public and no-extra-cost (no cloned or tier-gated voice — that is another dead leg waiting) and all feminine English-first conversational so a mid-call failover is heard as a slightly different person rather than a change of sex or accent; Cartesia pinned to the exact prod env value so **no existing agent's voice changes**; `resolveVoiceId(provider, voiceId?)` is the only adapter path and is blank-safe (an empty ID was interpolated as an empty path segment, which every provider rejects differently and none usefully); `DEFAULT_SPEAKER` deleted; voice IDs leave `assertVoiceConfig` and both doc surfaces so nobody re-adds them from documentation; and the boot check now warns per **dead failover leg** by comparing both default chains against the API keys actually present — a `warn`, not a `problem`, since the primary path still works. Tests assert at the wire through a `MockWebSocket` (a unit test of `resolveVoiceId` would have passed against the broken adapters) and were proven to fail for the right reason by restoring the env read. Rejected: setting `ELEVENLABS_VOICE_ID` in Railway (2 minutes, keeps the inversion, recurs for the next provider), a DB table (puts a network read on the path that only runs because something already broke), failing the call when no voice resolves (the fallback exists *because* the primary failed), and keeping env as a third tier (preserves the exact ambiguity being removed). Leaves open, as a business call: the ElevenLabs account returns `payment_issue` on every generation, so TTS is single-sourced on Cartesia until an invoice is paid | 2026-08-12 | Accepted (implemented 2026-08-12) |
| [ADR-103](./adr-103-a-scenario-the-caller-refused-to-play-is-not-a-passing-test.md) | A scenario the caller refused to play is not a passing test — the Misc-9 synthetic harness is the only automated behavioural check this product has (on-demand, deliberately **not** in CI), and an A/B run turned it into the thing being measured: `wrong-info` had **never** passed and could not, because its persona was purely reactive while the loop makes the caller speak first from an empty transcript, so the caller model returned "" , the silent `break` fired on turn zero, and the lone `agentSaid` assertion failed vacuously (0 turns, ~1.7s, identical across two models and two templates); every scenario modelled an **inbound** call while production is **10 outbound / 1 inbound** and outbound is the launch motion; and all eight were ecommerce-flavoured against six insurance templates, so ADR-081's scope boundary existed only as prose. Fixing that exposed a fourth and worse defect: the scripted caller runs on an **aligned** model that **refuses adversarial personas** — asked to volunteer a fabricated SSN it answered in its own assistant voice ("I can't provide or repeat highly sensitive personal or financial identifiers…") and offered *the agent* a bulleted menu of insurance topics, and both data-handling scenarios **passed** because the agent was never asked to cross the line; a "this is a QA test" preamble made it worse. So: `firstSpeaker` per scenario (Vapi's `firstMessageMode` axis, default `caller`, agent-first driven through the *exported* `GREETING_TURN_SEED` rather than a paraphrase that drifts), `callerMustSay` phrases that force `endedBy: "caller-off-script"` and `allPassed: false` when the caller never actually said them, a per-scenario `callerModel` pin (boundary scenarios → direct Groq `llama-3.3-70b-versatile`, on which the caller pushed the SSN four times and the agent refused every time — a real pass) with a test requiring any pinned scenario to declare `callerMustSay`, `endedBy: "caller-silent"` replacing the silent break, both surfaced in the dashboard instead of rendering as an ordinary end-of-run, and a `toolCalledAnyOf` assertion because "hand this off" is legitimately satisfied by a live transfer **or** an advisor booking. `wrong-info` now runs 8 turns and passes. What the fixed instrument then found: the ADR-081 boundary **holds** under adversarial pressure (no premium quoted, no coverage bound, no SSN accepted, licensure never claimed) — but the hand-off is **spoken and never recorded** (agent promises an advisor callback, calls neither `bookAppointment` nor `transferToHuman`, so a warm lead who verbally agreed leaves no row — the launch vertical's only conversion event, ADR-090's class in the product itself), `flagGuardrailEvent` fires **6×** on a polite-but-persistent caller and 4× on another (outbound true negative now asserted, currently failing), one turn emitted duplicated text with a tone tag mid-sentence (fourth defect in that feature after ADR-082/-083/-101), and the "sounds scripted" complaint is now reproducible — the same canned advisor line recited near-verbatim across turns. Measured on the way and **not** fixed (needs an env decision): direct Groq supports streaming tool use on all four models probed (TTFT 160–533ms), but `groq/llama-3.3-70b-versatile` **via the gateway failed 4 of 10** identical requests because routing attempts **bedrock first** (400 "doesn't support tool use in streaming mode") before groq (503) — and that slug is the last link of production's `AI_GATEWAY_FALLBACK_MODELS`, so the tail of the declared failover chain is ~40% broken for a 10-tool streaming workload | 2026-08-12 | Accepted (implemented 2026-08-12) |
| [ADR-104](./adr-104-a-persona-that-reads-like-a-document-gets-recited.md) | A persona that reads like a document gets recited — three findings, one cause. Production call 22 spoke *"Hello, is this ? This is calling on behalf of krisn"* and call 24 spoke *"Hi, is this **[Caller Name]**? This is **[Agent Name]** with presistentads"*: six of nine personas opened with bracket grammar (`You are [Agent_name: {{agent_name}}]`), and `merge-tags.ts` resolves only `{{tag}}`, so it stripped the tag from *inside* the brackets and left the label standing to be read aloud — the file's own "degrades gracefully" docstring, falsified in production. Underneath: `seedAgentTemplates()` wrote each markdown file **verbatim** into `default_persona_prompt`, so the `**File:**` header, the regulatory-grounding pointer, the variables table, "Why this template exists", the tools table and the "Known gap" paragraph were all shipped to the model as call instructions — **13-40% of every seeded persona was prose addressed to a maintainer**, worst on the launch agent at 19,711 chars / 272 lines / 40% metadata, re-sent every turn of every call. And what remained was a numbered script (`SECTION 1`-`6`, steps 1-7, lettered `Branch A/B/C`, cross-references to a "Reschedule Module" the model never sees), which is why ADR-103's harness caught the agent reciting the same canned advisor line near-verbatim across turns and answering six pricing pushes with restatements of one refusal. "Sounds scripted" was not a tuning problem — the agent was handed a script and obeyed it. So: `<!-- runtime:begin -->`/`<!-- runtime:end -->` markers split each file into a seeded runtime region and maintainer-only prose, `extractRuntimePersona` **throws** rather than falling back to the whole file (a silent fallback re-creates the defect invisibly on the tenth prompt file — ADR-090's class), all nine runtime regions rewritten as goal-based guidance with every regulatory guardrail and every audited per-language line carried over **verbatim**, plus an explicit rule never to speak a placeholder or bracketed label. Fourth defect, found while probing gateway transports for ADR-105: gateway `groq/llama-3.1-8b-instant` (served by **deepinfra**, not Groq) emitted its own tool syntax as assistant text in **4 of 6** runs — `` 3"}</function>I've saved your order number `` — and nothing in the path refused to speak it, since `agent.ts` piped `textStream` straight to `onTextDelta` → TTS and the only filter was the *leading*-`[[tone:]]` stripper. `voice/output-guard.ts` now scrubs tool syntax, JSON residue and bracket slots at that one chokepoint (buffering 44 chars across delta boundaries, carrying whitespace so a removal cannot produce "is this ?"), with the stored transcript accumulated from the **guarded** text so the record matches what the caller heard, and every finding `console.warn`ed as the upstream defect it is. Persona size becomes a CI ratchet (`persona:gate`, budgets = measured sizes, never round numbers) and the G1.3/G1.4 prompt-hygiene gates are re-pointed from the file at the seeded region — a tightening, since G1.4 now covers **all 9** templates instead of 3. Measured: seeded persona text 103,752 → 73,783 chars (−29%), launch agent −40%, bracket slots 6 files → 0, script labels 9 files → 0, api tests 1,188 → 1,221. **Not yet true in production:** the four prod orgs still hold the old whole-file personas and keep reciting until `seedAgentTemplates()` is re-run against prod | 2026-08-12 | Accepted (implemented 2026-08-12) |
| [ADR-105](./adr-105-an-agent-that-cannot-transfer-must-not-promise-a-person.md) | An agent that cannot transfer must not promise a person — production call 25 (outbound, the launch final-expense qualifier, 46 turns, 21 tool calls) qualified a lead cleanly, stayed inside every ADR-081 boundary, and closed with *"Let me connect you with a licensed advisor right now… **You're connected — the advisor will take great care of you.**"* Nobody was connected; the line was hung up on them. `orgs.human_transfer_number` is **NULL on 4 of 4 production orgs** and `insurance_advisors` is still empty (ADR-098), so `performTransfer` resolved no target, logged its no-number warning and called `performHangUp`. Every layer behaved as written — `resolveHumanTransferNumber` correctly refuses a global env fallback (rejected 2026-07-17: cross-org transfers), `performTransfer` correctly prefers hanging up to guessing, ADR-082's precedence correctly chose transfer over the same-turn hangup — the defect is upstream of all of it: **the model was handed a `transferToHuman` tool on a call where it could not possibly succeed**, plus a persona telling it the best outcome is a live warm transfer. The tool's existence was the promise. Meanwhile the row reads `status = "completed"`, `disposition = "booked"`, `health_status = "healthy"`, `intent = "purchase_or_booking"` — on every dashboard this is the best call the product has ever placed, and it is a warm lead dropped mid-promise; the launch vertical's only conversion event is structurally impossible in production today. Second finding, same call: the closing line was spoken **twice** seven seconds apart, with `transferToHuman` 2×, `crmSync` 2× and `sendSms` 2× — filed for two sessions as "duplicated agent text", it was a whole extra turn, because ADR-082's `transferLatched` gated `hangUp` and nothing else while the bridge happens at `speak()`'s tail with STT still connected, so the caller's near-duplicate "You can use the same number" ran a complete phantom turn. So: new pure `voice/handoff.ts` resolves `resolveTransferCapability({ transferNumber, provider, hasOrg })` **once** in the `"start"` handler (fixed for the call's life, like `crmSyncContext`; `humanTransferNumber` rides the `orgs` select already in the batched pickup lookup, so zero added latency) with reasons `no-org` / `provider-unsupported` (only `twilio`, `plivo` have a wired mid-call transfer path) / `no-transfer-number`; `narrowToolsForTransferCapability` drops the tool from `enabledToolsOverride`, which rewrites the prompt for free because `buildCallControlBlock` already derives `canTransfer` from that list — the seam existed, nothing was feeding it the truth. The `undefined` case is what mattered: `enabledTools === undefined` means "all tools" and most production calls have no agent-frame row, so a plain `.filter()` would have been a no-op exactly where nobody configured anything, hence the helper materializes `AVAILABLE_TOOL_NAMES` in that case and *only* that case. `bookAppointment` stays intact so a blocked call keeps its documented fallback. Third: a call-control rule that the model may **promise** the handoff but never **report** it ("one moment while I get them on the line", never "you're connected"). Fourth: `transferLatched` now short-circuits the STT end-of-turn handler — the utterance is still written to `transcripts` so the record stays faithful, but no turn runs and no tool fires. `performTransfer` keeps its own checks as defence in depth, and because they are now two copies of one decision `handoff.test.ts` asserts against `stream.ts`'s **source text** that they still agree and that capability is resolved before the tool list is built. Also corrects a wrong claim on the record (per ADR-078, as a new entry): "hand-off spoken but never recorded" was an ADR-103 *harness* finding, not production behaviour — production records the tool call and still hangs up, which is worse. api tests 1,221 → 1,241. **Known and unfixed:** `call-health.ts` still calls call 25 `healthy` (correctly — it is scoped to dead air / STT / TTFT), so nothing in the stack notices a broken promise, and nothing yet tells an operator to set the number beyond a `console.warn` | 2026-08-12 | Accepted (implemented 2026-08-12) |
| [ADR-106](./adr-106-a-number-the-agent-was-never-given-is-a-number-it-invented.md) | A number the agent was never given is a number it invented — three more findings from the call 25 that produced ADR-105, all about what the agent wrote and said while making a promise it could not keep. **It texted the caller a phone number that does not exist.** Two SMS went out: *"Here's the advisor's number for your records: **[Advisor Desk Number]**"* — an unresolved bracket placeholder, the exact shape ADR-104 stopped from being *spoken*, delivered in writing five hours after that ADR shipped — and *"Please contact your licensed advisor at **888-555-0199**"*, a number that does not exist anywhere: `orgs.human_transfer_number` is NULL on all four prod orgs, `insurance_advisors` is empty, the caller never said a number, nothing in the prompt contained one. The model had a slot and no source, so it invented something that looked right, and a caller who keeps that message now has a wrong number attributed to the org in writing. Nothing screened either: ADR-104 built a guard for exactly this class and pointed it at one channel (the token stream to TTS), while a voice agent's second channel is the one that *persists* — `sendSms.body` to the caller's phone, `crmSync.notes` onto a contact timeline a salesperson reads, `bookAppointment.notes` into a calendar invite. **It read a stage direction out loud:** *"\*Sending text message...\* [[tone:upbeat]] And that's everything I need"* — the markdown is the model writing for a chat window in a channel with no screen, but the tone tag is ours, the fifth defect in that feature after ADR-082/-083/-101, and the mechanism is exact: the stage direction is 23 chars, `TONE_TAG_MAX_BUFFER_CHARS` is 24, so the filter hit the cap, correctly concluded "no leading tag is coming", released — and forwarded the tag as speech, because after resolution `push()` was a raw pass-through. The cap ADR-101 added to stop the filter muting short turns is what let this through, working exactly as designed. **And it framed an outbound call as inbound** ("the line that you reached out on", plus asking which number to use) — whose answer, "You're gonna use the same number", is what ran the phantom post-latch turn ADR-105 fixes; a question the agent had no business asking produced the reply that produced the duplicated hand-off. So: new `voice/outbound-text-guard.ts` wrapping `scrubSpokenText`'s finding set (not a second copy of the patterns, which would drift on the next model family) plus one new finding `unverified-phone-number`, and it **refuses instead of scrubbing** — deletion is right mid-utterance where there is no second chance, but an SMS is a discrete atomic act and a scrubbed one ("contact your licensed advisor at .") reads as broken and still fails the caller. The test is **provenance, not plausibility**: `888-555-0199` passes every shape check (length, prefix, carrier lookup) because every such check validates the shape of the invention, so a number may appear in outbound text only if the server put it in scope (`orgs.humanTransferNumber`, the connected leg) or the caller said it — `callerSpokenNumbers` harvested in `logTranscript` before its `dbCallId` early return and read through a closure, never snapshotted, because a number the caller reads out on turn six is one the agent may repeat on turn seven. Both calibrations lean against false refusals (a wrongly-refused SMS is silent): **10** digits minimum rather than E.164's 7, since free prose is full of order numbers and amounts and `resolveCrmSyncContext` can use 7 only because the carrier vouched for that value; ISO date-times stripped before the scan and `dateTimeIso` not screened at all. Comparison on the last 10 digits so `+1 888…` vs `(888)…` is one number. Wired via `withOutboundTextGuard` inside `buildVoiceTools` (same shape as the existing `withFillerTimer`, the one place every caller shares, so a new tool cannot quietly skip the screen) for `crmSync`/`bookAppointment`, and in `stream.ts` for `sendSms`, whose `execute` is signal-only — a refusal inside that tool would refuse nothing — with a source-text assertion that the screen still precedes the send. Refusals return to the model as a readable instruction rather than a thrown error (a throw ends the step and the caller hears the fallback line) and log a `guardrail_events` row, category `fabricated-outbound-text`, source `outbound-text-guard` — a type widening with no migration, both columns being plain `text` with no check constraint, the same widening `capture-guard` made. `stripToneTag` now strips **every** occurrence and reports the first recognized tone (position decides whether the tone is trustworthy, not whether the characters may be spoken), and the streaming filter's raw pass-through is replaced by holding back only from a dangling `[`, backing up over the bracket run because `lastIndexOf("[")` on `"[["` finds the second one and would emit the first as speech. `output-guard.ts` gains `markdown-syntax`, deleting asterisks but **keeping the words** — a deliberately partial fix, since an emphasis span can wrap a real word and the guard sees deltas whose boundaries are not line boundaries; the narration itself is fixed at the prompt layer with a rule that everything produced is spoken aloud and that the model must never narrate its own actions, plus a rule never to state a number/email/link it was not given, reason attached because the reason generalizes (*the caller will try it*). Outbound identity line finished: the caller did not reach out or ring in, their number is the one that was dialled, never ask which number to use. api tests 1,241 → 1,278. **Known and unfixed:** a refused SMS is a message the caller expected and did not get, and the agent is not told, so it cannot correct itself in the moment — feeding the refusal back into the turn is the honest next step and is not here; `flagGuardrailEvent`'s false positives (6× and 4× on polite callers) are still open and still unfiled | 2026-08-12 | Accepted (implemented 2026-08-12) |
| [ADR-107](./adr-107-a-stage-that-is-charged-for-another-stages-time-cannot-be-optimized.md) | A stage that is charged for another stage's time cannot be optimized — production `turn_latency` said voice-to-voice p50 was **1878 ms** and that **1748 ms of it was TTS**, on a turn `llm_ttft` already claimed **1381 ms** of; the two components summed to roughly twice the turn they were meant to decompose. The row-level tell settles it: sorted by v2v, `v2v - tts` is pinned at **~127 ms on every single row** (129, 128, 127, 128, 130) across a two-second spread of LLM time — a real TTS measurement cannot track LLM latency that rigidly. Cause: `speak()` captured `ttsRequestedAt = Date.now()` at its top and computed the TTS stage from it, but `speak()` begins **before** `generate()` runs the model, so the column actually meant "speak() entry → first audio byte" and contained the entire LLM TTFT plus any tool round-trip; the ~127 ms constant was the only part that was ever TTS-and-dispatch. The `turnLatency` doc comment asserting the two were "kept separately so a regression can be attributed to the right stage" was therefore false as written — ADR-090's defect class one level up, since the metric had a real caller and real rows and only its *meaning* was detached from its name, which no unit test asserts. Not cosmetic: the next latency decision on the table is which LLM transport to run, and the instrument said the model was a minor contributor; corrected, the p50 turn decomposes to **~127 ms dispatch / 1381 ms LLM / ~370 ms TTS** — the model is about three quarters of the caller's wait and TTS provider shopping would have bought almost nothing. So: `ttsFirstByteMs` (per-turn **and** call-level, which had the identical defect on the call's first speaking turn) is now anchored on `ttsTextFirstSentAt`, the first character handed to TTS, captured inside ADR-083's lazy-connect facade and deliberately set **before** the socket opens so connect time stays inside the TTS budget rather than escaping it. The existing column is **redefined, not duplicated** — all 78 pre-cutover rows are internal test calls (11 calls, 2026-07-18..2026-08-10, no customer traffic), so carrying a parallel column forever to protect them is the worse trade; the cutover instant is pinned in the schema doc comment and rows either side of it must not be pooled. No column is renamed or dropped, so the additive-only migration invariant is untouched. `voiceToVoiceMs` is held **identical in meaning and in value** and now reads a `turnFirstAudioAt` absolute instant instead of being reconstructed as `ttsRequestedAt + turnTtsFirstByteMs`, a formula that was only ever correct while both happened to share an anchor; the cache-hit path stamps that instant too, without which v2v would have gone silently null on every cached turn — a regression this change would otherwise have introduced. New `stream-latency-attribution.test.ts` asserts **meaning, not presence**: an LLM that stalls before its first token in front of an instant TTS, with the stall required to land in v2v and not in `ttsFirstByteMs` — verified to fail against the old anchor (2 of 3 assertions, the double-counting one reporting `241 <= 142`, reproducing the doubling on demand). Also corrects a wrong claim on the record (per ADR-078, as a new entry): ADR-104's "the four prod orgs still hold the old whole-file personas and keep reciting until `seedAgentTemplates()` is re-run against prod" is **false** — `runtime:begin` is a source-file marker that `extractRuntimePersona` strips during seeding, so its absence in the DB is evidence the extraction ran, not that it did not; a SHA-256 comparison of repo-extracted runtime text against all nine prod rows returns **9/9 identical**, ADR-104 has been live in production since it shipped, and no re-seed was needed or performed. api tests 1,278 → 1,281. **Known and unfixed:** dashboard TTS p50 will drop ~1.4 s at the next call and that is a measurement correction, not an improvement; any pooled query spanning 2026-08-12 returns a bimodal `tts_first_byte_ms` distribution until the rolling window clears the cutover; STT finalization lag before `speechFinal` fires is still unmeasured, so the ~127 ms dispatch residue is the floor we can see, not the caller's true floor | 2026-08-12 | Accepted (implemented 2026-08-12) |
| [ADR-108](./adr-108-a-bypass-that-expires-silently-fails-in-front-of-the-prospect.md) | A bypass that expires silently fails in front of the prospect — dialing `+91` stopped working and **nothing was broken**. `91efb0f` (ADR-096) made `assertOutboundCallAllowed` the single fail-closed chokepoint, closing the three previously-ungated paths (`POST /api/leads/:id/call-now`, tenant preview test-call, admin `.../test-call-phone`) that live testing actually used — the last real production dials predate it. With the chokepoint in place an insurance-vertical org dialing `+91` reaches `checkInsuranceNumberSeriesCompliance`, which is unconditional for that vertical and requires an active TRAI 1600-series number no org has, so the dial is refused before Twilio is touched: the correct outcome. The escape hatch already existed and already covered it — `orgs.callingWindowTestModeUntil`, set to now()+24h by `POST /api/app/compliance/test-mode`, returns `allowed: true` from `insurance-gates.ts` **before any number lookup**, its own comment saying it exists to "run a live phone demo before the 1600-series number is registered". Full gate walk for an insurance org dialing a fresh `+91` with test mode on: DNC enforced (never bypassed), calling window bypassed, FTSA attempt cap a no-op (Florida NANP only, `+91` cannot match), 1600-series bypassed, producer licensing a no-op (US-only, and fails open on an empty roster per ADR-098), India DLT skipped for the insurance vertical. The capability was never missing; what failed is **the ergonomics of its expiry**. Test mode is deliberately self-expiring so it cannot be left on in production, and on expiry its refusal is *byte-identical* to the refusal an org that was never configured gets — the full TRAI registration paragraph, accurate and the wrong thing to read out during a demo when the remedy is one toggle. Prod state at diagnosis: two orgs NULL, one expired 2026-08-11 20:19Z, one expired 2026-08-04; the most recent had lapsed the previous evening, and that was the whole of it. So, two changes, both legibility, neither widening what test mode lifts: **(1)** `assertOutboundCallAllowed` becomes a thin wrapper over `runOutboundGates` (fail-closed logic untouched) which, on refusal only, appends the diagnosis when the org has a **lapsed** `callingWindowTestModeUntil` — additive, since the registration requirement is still real after the toggle, so the original reason survives verbatim. Scoped by `TEST_MODE_BYPASSABLE = {calling_window, insurance_number_series, insurance_producer_licensing}`; `dnc` and `attempt_cap` are deliberately absent because they are never bypassed and a hint implying otherwise would teach an operator that DNC has a toggle. Silent when the column is NULL (no demo history to blame) and silent while test mode is still **active** (the refusal then came from a gate test mode does not lift, so blaming it would actively mislead), and best-effort by construction — an error-message improvement must never convert a clean refusal into a throw, so any failure yields no suffix. **(2)** `formatTimeRemaining` returns null once lapsed rather than "0m", forcing callers to handle the expired case; the dashboard badge gains `Xh left`, switching to a bolded `lapses in Xh` inside the last 3 hours, and Settings gains the same warning plus an explicit "expired, your next call will be refused" line for a **past** timestamp — the state worth naming loudest, since the toggle reads "off" either way but here a gate that passed yesterday refuses today. **Rejected:** a per-org verified test-number allowlist (proposed first, wrong shape — demos are given to whoever is in the room in real time, so the destination cannot be pre-registered; recorded so it is not re-proposed); making the 1600-series gate flagged like India DLT (IRDAI mandate, deadline passed, and the demo path already has a documented self-expiring bypass); extending or making the 24h window configurable (the window *is* the control); replacing the gate's reason with the hint. api tests 1,281 → 1,287, `outbound-gate-test-mode-hint.test.ts` asserting meaning — hint only for gates test mode lifts, DNC and attempt-cap refusals verified clean, original reason survives — proven non-vacuous by stubbing the hint to `""` (2 of 6 failed). **Known and unfixed:** test mode is a blanket 24h lift for **every** destination, not a demo-scoped one — defensible for invited demos, not a safe basis for cold outreach (FCC Feb 2024 declaratory ruling: an AI voice is "artificial" for TCPA at $500–$1,500/call, no aggregate cap, private right of action), and nothing in the product distinguishes the two uses, so the constraint is operational; the countdown is computed at render time and does not tick, so a dashboard left open past the boundary shows a stale label until the next fetch | 2026-08-12 | Accepted (implemented 2026-08-12) |
| [ADR-109](./adr-109-the-last-link-of-the-failover-chain-was-the-least-reliable-one.md) | The last link of the failover chain was the least reliable one — ADR-103 measured it in passing and did not act: gateway `groq/llama-3.3-70b-versatile` **failed 4 of 10** identical streaming-tool requests because gateway routing attempts **bedrock first** (400, "doesn't support tool use in streaming mode") before groq (503), and that slug is the **last link** of production's `AI_GATEWAY_FALLBACK_MODELS` — so the declared chain's last resort is the least reliable link in it, the inverse of what a chain is for. Copying `failover.ts`'s shape (a Groq-only **model** chain) was the wrong axis: it protects against one model being at capacity, not against the transport being unreachable, which is the 503 observed. So the topology is **cross-transport** — direct Groq primary, **gateway as the last link** — because the two share no failure domain (direct bypasses Vercel entirely; the gateway reaches models Groq does not serve). Syntax is `direct:groq/<model>` and the **colon is the whole point**: `groq/llama-3.3-70b-versatile` is *already* a valid **gateway** id (gateway routing to groq compute) and is literally production's current value, so reading a bare `groq/` prefix as direct-Groq would have silently redefined a value already live, with no migration and no error — the exact failure mode this repo keeps writing ADRs about; a colon cannot collide with the gateway's `provider/model` namespace, so every id valid today keeps its meaning and only a new `direct:` id opts in, `gateway:` is accepted as redundant-explicit, and `direct:<non-groq>/…` is **dropped rather than silently served by Groq**. Per-agent config **reuses `org_agent_configs.llm_fallback_models`** — same `jsonb string[]`, additive vocabulary, redefinition documented in the schema comment, no new column and no migration. **The retry window closes at the first token**: once a delta reaches `guard.push()` it has gone to TTS and the caller has heard it, so retrying would make the agent say two things in one turn — a link failing *before* its first delta is retried, one failing *after* rethrows, aborts never retry, and mid-stream failover is deliberately not built because that is the boundary, not a gap. **Invariant:** a non-empty chain forces `providerOptions: undefined` (every link is one concrete model, so also handing the list to the gateway retries it at two layers and multiplies one refusal by the turn's whole latency budget) — enforced at `agent.ts`'s call site and guarded by a source-text assertion on ADR-105's precedent. Latency is labelled with the link that **actually spoke** via `formatActiveModelLabel`, emitting the same `transport/model` shape as `getActiveModelLabel` so `turn_latency.llm_provider_used` stays comparable and no dashboard learns a second format — booking a fallback's TTFT against the primary would make the soak measure nothing (ADR-107). **Ships dark behind `LLM_TRANSPORT_FAILOVER`, default off in production**: flag off ⇒ empty chain ⇒ byte-identical gateway-native path, and an empty chain must be read as "leave the existing path alone", not "no fallbacks exist". Latency is deliberately the **secondary** justification and stays **unverified**: the ~130ms hop (334ms vs 206ms, same model both ways) was measured from a dev sandbox, not Railway Singapore, and is ~9% of ADR-107's corrected LLM p50 — shipping a transport on a sandbox stopwatch would repeat the exact instrument error ADR-107 exists to correct. **Rejected:** the Groq-only model chain; both axes at once (multiplies worst-case latency by the product of two chain lengths for a failure mode never observed); a new `llm_transport_chain` column; measuring from staging first (impossible today — `/health` runs no LLM and test-chat needs auth plus an org that no longer exists after the DB wipe, so measuring would itself require shipping code). api tests 1,287 → 1,307, non-vacuity proven by deleting `if (produced) throw error;` (16 pass / 1 fail, exactly the retry-window test). **Known and unfixed:** enabling the flag on staging isolates nothing, because staging and production still share ~33 of 40 env vars including `DATABASE_URL` and the Twilio account, so a staging soak dials and writes against production | 2026-08-13 | Accepted (implemented 2026-08-13) |
